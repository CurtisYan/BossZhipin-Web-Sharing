# v0.4.4

## 新功能

- 插件主页面新增“职位文本按钮”开关。
- 开启后，职位页右下角会显示“复制职位”按钮。
- 点击后直接复制职位名称、公司、薪资、地点、条件、招聘人、工作地址、完整职位描述和职位链接。

## 体验改进

- 文本复制复用长图的职位解析结果，不触发二维码获取或图片绘制。
- 同时开启职位文本和调试模式时，悬浮按钮会自动顺序排列，避免互相重叠。
- 补充异步剪贴板失败时的兼容复制方案。
- 从职位列表页复制文本时，自动匹配当前职位的详情页链接，不再导出职位列表地址或安全检查参数。

## 验证

- JavaScript 语法检查通过：`content.js`、`popup.js`、`background.js`。
- `manifest.json` 格式检查通过，版本号为 `0.4.4`。
- 发布包内容检查通过，仅包含扩展运行所需的 7 个文件。

## 安装包

- `boss-job-share-extension-v0.4.4.zip`
